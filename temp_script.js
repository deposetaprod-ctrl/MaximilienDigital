
(()=>{document.documentElement.classList.add('js');const reduced=matchMedia('(prefers-reduced-motion: reduce)'),mobile=matchMedia('(max-width: 600px)');const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(e=>io.observe(e));const services=document.querySelector('.services'),track=document.querySelector('.track'),progress=document.querySelector('.progress i'),hero=document.querySelector('.hero'),desktop=document.querySelector('.desktop'),phone=document.querySelector('.phone'),note=document.querySelector('.note');let ticking=false;function draw(){ticking=false;if(reduced.matches){[desktop,phone,note,track].forEach(e=>e.style.transform='');return}const y=Math.min(Math.max(-hero.getBoundingClientRect().top,0),1000);desktop.style.transform=`translateX(${-y*.12}px) translateY(${y*.04}px) rotate(${-3-y*.008}deg)`;phone.style.transform=`translateX(${y*.20}px) translateY(${-y*.05}px) rotate(${10+y*.016}deg)`;note.style.transform=`translateX(${-y*.24}px) rotate(${-10-y*.015}deg)`;if(!mobile.matches){const sticky=document.querySelector('.services-sticky'),r=services.getBoundingClientRect(),distance=services.offsetHeight-sticky.offsetHeight,p=Math.max(0,Math.min(1,(83-r.top)/distance)),overflow=Math.max(0,track.scrollWidth-innerWidth);track.style.transform=`translateX(${-p*overflow}px)`;progress.style.width=`${p*100}%`}else track.style.transform=''}function schedule(){if(!ticking){requestAnimationFrame(draw);ticking=true}}addEventListener('scroll',schedule,{passive:true});addEventListener('resize',schedule);reduced.addEventListener('change',schedule);draw();})();
